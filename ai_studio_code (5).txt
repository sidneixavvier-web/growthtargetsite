import { Search, MapPin, Instagram, Play } from "lucide-react";

const services = [
  { title: "Google Ads", desc: "Captura de demanda qualificada no maior buscador do mundo.", icon: Search },
  { title: "Google Business", desc: "Domine as buscas locais e apareça no topo do Maps.", icon: MapPin },
  { title: "Meta Ads", desc: "Escala agressiva através de criativos que vendem no Instagram.", icon: Instagram },
  { title: "TikTok Ads", desc: "Aproveite a nova atenção do mercado com anúncios nativos.", icon: Play },
];

export default function Services() {
  return (
    <section id="servicos" className="py-24 px-6 bg-brand-black">
      <div className="max-w-7xl mx-auto">
        <h2 className="font-montserrat text-4xl mb-16 text-center">NOSSOS <span className="text-brand-orange">CANAIS DE ESCALA</span></h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {services.map((s, i) => (
            <div key={i} className="group p-8 bg-white/5 border border-white/10 rounded-2xl hover:border-brand-orange/50 transition-all duration-500">
              <s.icon className="text-brand-orange mb-6 group-hover:scale-110 transition-transform" size={40} />
              <h3 className="text-xl font-bold mb-4">{s.title}</h3>
              <p className="text-gray-400 text-sm leading-relaxed">{s.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}